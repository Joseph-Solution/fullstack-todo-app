{
  "family": "todo-frontend",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::248729599833:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::248729599833:role/ecsTaskRole",
  "containerDefinitions": [
    {
      "name": "frontend",
      "image": "248729599833.dkr.ecr.ap-southeast-2.amazonaws.com/joseph-solution/fullstack-todo-app:frontend-IMAGE_TAG",
      "portMappings": [
        {
          "containerPort": 4567,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "environment": [
        {
          "name": "NODE_ENV",
          "value": "production"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/todo-frontend",
          "awslogs-region": "ap-southeast-2",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:4567 || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      }
    }
  ]
}
